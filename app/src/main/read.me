2018.7.19
实现功能如下

1. ui层和viewmodel绑定并请求数据，viewmodel获取数据并将数据映射到ui层的view

待实现功能：

1. viewmodel将对ui操作的实现与从服务器或本地数据库处理数据的操作分离。通过动态代理的方式

2. 从服务器获取数据缓存到本地服务器

3. 实现recycleview 的万能适配器

4. 实现自定义recycleview 的上拉加载功能

5. 网络出现异常 如何更新ui？eventbus?

6. 下拉刷新操作

-----
待整理的内容：
1. 闭包
2. let apply的使用
3. ？的含义
4. lateinit和by  lazy的去别
?
---
1. 用在变量后面表示  变量可为空null，否则不能位空

4. lateinit:如果用来声明一个变量，那么这个变量的值不能为null 判断该值时也不为空，之后需初始化后使用类的变量需要通过创建对象调用

5. 伴生对象使用外部
